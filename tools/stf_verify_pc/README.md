# stf_verify_pc

Verifies that there are no unexplained PC discontinuities in a trace

```
usage: stf_verify_pc [options] <trace>
    -h, --help        show this message
    -V, --version     show the STF version the tool is built with
    <trace>           trace in STF format
```
