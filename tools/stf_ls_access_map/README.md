# stf_ls_access_map

Collects memory access statistics by cache line and 4k region

```
usage: stf_ls_access_map [options] <trace>
    -h, --help        show this message
    -V, --version     show the STF version the tool is built with
    -u                skip non user-mode instructions
    <trace>           trace in STF format
```
